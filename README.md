# honeypot

note: this project is still in development, you may find bugs

## get started:
1. start the backend:
   ```bash
   cd backend
   docker compose up --build
2. view the dashboard in the frontend:
   ```bash
   cd frontend
   npm install
   npm start
